# Security policy

Do not commit credentials, tokens, private keys, personal data, or populated environment files. Report suspected vulnerabilities privately to the repository owner rather than opening a public issue.

The Phase 1 Tauri shell has no filesystem, shell, or operating-system capabilities. Future native commands must be narrow, validated, permission-scoped, and tested before being enabled.
