# Used by the desktop webview when calling the local API.
VITE_API_BASE_URL=http://127.0.0.1:8000

# Comma-separated origins allowed to call the API during development.
ASSISTANT_CORS_ORIGINS=http://localhost:1420,tauri://localhost,http://tauri.localhost

